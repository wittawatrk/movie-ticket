export type FindProvincesOutput = {
  code: string;
  name: string;
}[];

export type FindDistrictsOutput = {
  code: string;
  name: string;
}[];

export type FindSubDistrictsOutput = {
  code: string;
  name: string;
}[];

export type FindProvinceOutput = {
  code: string;
  name: string;
};

export type FindDistrictOutput = {
  code: string;
  name: string;
};

export type FindSubDistrictOutput = {
  code: string;
  name: string;
};
